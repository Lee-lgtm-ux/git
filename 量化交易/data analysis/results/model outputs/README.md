# Model Outputs

这里存放质量趋势模型的输出结果。

- `model_outputs_quality_trend_change_hgb_v1/`：非线性 HGB 模型输出。
- `model_outputs_quality_trend_change_ridge_v1/`：线性 Ridge 基准模型输出。

每个模型目录通常包含 metrics、预测结果、分组表现、IC 序列和图表。
