# Scripts

这个目录把 `data analysis` 相关 Python 脚本按功能归档。

## 目录

- `data builders/`：构建训练数据、质量因子数据、代表性股票池和前端股票池数据。
- `models/`：训练市场收益模型和财务质量趋势模型。
- `validation/`：验证模型信号和未来收益之间的关系。
- `strategies/`：交易策略和配对交易工具。

建议从 `量化交易/data analysis` 目录作为工作目录运行脚本，这样默认输入输出路径会和 README 中一致。
