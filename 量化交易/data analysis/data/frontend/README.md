# Frontend Data

这里存放前端页面直接通过 `<script>` 加载的内置数据文件。

- `quality_model_artifact.js`：质量趋势模型在页面中使用的模型数据。
- `shanghai_index_data.js`：上证指数内置数据。
- `pair_pool_data.js`：配对交易 SSD 功能使用的代表性股票池数据。

这些文件会影响 `index.html` 的交互功能，不建议随意删除。
